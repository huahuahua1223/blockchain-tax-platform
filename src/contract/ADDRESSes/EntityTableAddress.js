const contractAddrArray = require('../../contract/contract-addresses.json');
const entityTableAddress = contractAddrArray.EntityTableAddr;

const EntityTableAddress = entityTableAddress;
export default EntityTableAddress;